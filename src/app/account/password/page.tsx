'use client';import{useState}from'react';import Link from'next/link';export default function PasswordPage(){const[c,setC]=useState(''),[n,setN]=useState(''),[v,setV]=useState(''),[m,setM]=useState('');async function go(e:React.FormEvent){e.preventDefault();if(n!==v)return setM('两次密码不一致');const r=await fetch('/api/auth/password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({currentPassword:c,newPassword:n})});const d=await r.json();setM(d.error||'密码修改成功')}return <main className="account-page"><section className="account-card"><Link href="/chat">← 返回工作台</Link><h1>账号设置</h1><div className="account-links"><Link href="/account/email">修改绑定邮箱 →</Link></div><form onSubmit={go}><label>当前密码<input className="input" type="password" required value={c} onChange={e=>setC(e.target.value)}/></label><label>新密码<input className="input" type="password" minLength={8} required value={n} onChange={e=>setN(e.target.value)}/></label><label>确认密码<input className="input" type="password" minLength={8} required value={v} onChange={e=>setV(e.target.value)}/></label>{m&&<p>{m}</p>}<button className="button button-primary">保存密码</button></form></section></main>}
