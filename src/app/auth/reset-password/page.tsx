'use client';import{useState}from'react';export default function ResetPage(){const[f,setF]=useState({email:'',code:'',newPassword:''}),[m,setM]=useState('');async function go(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/auth/reset-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...f,code:f.code||undefined})});const d=await r.json();setM(d.message||d.error||(r.ok?'密码已重置':''))}return <main className="account-page"><form className="account-card" onSubmit={go}><a href="/">← 返回首页</a><h1>找回密码</h1><input className="input" type="email" required placeholder="注册邮箱" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><input className="input" placeholder="验证码" value={f.code} onChange={e=>setF({...f,code:e.target.value})}/>{f.code&&<input className="input" type="password" minLength={8} required placeholder="新密码" value={f.newPassword} onChange={e=>setF({...f,newPassword:e.target.value})}/>} {m&&<p>{m}</p>}<button className="button button-primary">{f.code?'重置密码':'发送验证码'}</button></form></main>}
